# Pinned, not >=: the VPS reuses the stock radar's already-downloaded Chromium
# (chromium-1228) out of a shared ~/.cache/ms-playwright. A newer Playwright
# expects a different build and would re-download one, so both projects must
# agree on this version.
playwright==1.61.0
openpyxl>=3.1
python-dotenv>=1.0
# Used by telegram.py for the unattended run's ping, and by onedrive.py for Graph.
requests>=2.31
# Microsoft Graph sign-in for the staging workbook in OneDrive. Handles the
# device-code bootstrap and, thereafter, silent refresh-token renewal.
msal>=1.28
